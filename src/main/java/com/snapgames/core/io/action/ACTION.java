package com.snapgames.core.io.action;

public enum ACTION {
    NONE,
    HOME,
    START,
    CAPTURE,
    OPTIONS,
    UP, DOWN, LEFT, RIGHT,
    A, B, X, Y,
    TRIG_UPPER_LEFT,
    TRIG_LOWER_LEFT,
    TRIG_UPPER_RIGHT,
    TRIG_LOWER_RIGHT;
}